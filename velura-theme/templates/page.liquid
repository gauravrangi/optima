<section class="science">
  <div class="container-narrow">
    <div class="science-head">
      <h2>{{ page.title }}</h2>
    </div>
    <div class="rte" style="font-size:17px;color:var(--ink-soft);line-height:1.7">{{ page.content }}</div>
  </div>
</section>
